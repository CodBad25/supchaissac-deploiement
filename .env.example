# 🔐 Configuration d'authentification SupChaissac
# Copier ce fichier vers .env et ajuster les valeurs

# Mode d'authentification
# - DEV: Mode développement (password123 pour tous + admin dev)
# - PRODUCTION: Mode production (mots de passe sécurisés uniquement)
# - HYBRID: Mode hybride (password123 + mots de passe sécurisés + admin dev)
AUTH_MODE=HYBRID

# 🧪 Mode développement facile (true = password123 accepté)
DEV_EASY_LOGIN=true

# Mot de passe admin de développement (pour tests et démos)
ADMIN_DEV_PASSWORD=DevAdmin2024!

# Database Configuration
# SQLite (développement)
DATABASE_URL="sqlite://./data/supchaissac.db"
# PostgreSQL (production)
# DATABASE_URL="postgresql://username:password@localhost:5432/supchaissac"

# Session Configuration
# Générez une clé secrète forte pour la production
SESSION_SECRET="your-super-secret-session-key-change-this-in-production"

# Environment
NODE_ENV="development"

# Server Configuration
PORT=5000

# Logging
LOG_LEVEL="info"

# Instructions de configuration :
# 1. Copiez ce fichier vers .env
# 2. Remplacez DATABASE_URL par votre vraie URL PostgreSQL
# 3. Changez SESSION_SECRET par une clé secrète forte
# 4. Ajustez les autres paramètres selon vos besoins

# Exemples d'URLs de base de données :
# PostgreSQL local : postgresql://postgres:password@localhost:5432/supchaissac
# Neon (cloud) : postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/supchaissac?sslmode=require
# Supabase : postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
